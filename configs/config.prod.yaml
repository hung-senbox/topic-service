server:
  port: "8012"

database:
  active: "mongodb" # or "mongodb"

  mysql:
    host: "localhost"
    port: "3306"
    user: "root"
    password: ""
    name: "topic_service"

  mongodb:
    host: "localhost"
    port: "27017"
    # user: "Senbox"
    # password: ""
    name: "topic_service"

consul:
    host: "localhost"
    port: 8500
    
registry:
  host: "localhost"

